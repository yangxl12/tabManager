# TabNest 标签巢 · Chrome 扩展技术方案

> 依据《需求.md》+ demo（index.html，交互已全部用 mock 数据验证）产出。
> 原则：技术栈全用当前主流新版本，所有需求点必须有明确落地路径，不允许"再研究"项。

---

## 一、技术选型

| 层 | 选型 | 版本 | 理由 |
|---|---|---|---|
| 扩展规范 | Chrome Manifest V3 | — | Chrome 唯一现行标准，新标签页覆盖 = `chrome_url_overrides.newtab` |
| 框架 | React | 19.x | 已确认。并发渲染对列表/树更新友好，生态最全 |
| 构建 | Vite + @crxjs/vite-plugin | Vite 7 / crxjs 2.x | crxjs 提供 MV3 专用构建：自动处理 manifest、content script、HMR。新标签页改代码即刷新，无需手动 reload 扩展 |
| 语言 | TypeScript | 5.x（strict） | 全量类型，chrome API 用 `@types/chrome`（`@types/chrome` 或 `chrome-types`，取官方 chrome-types 最新） |
| 样式 | Tailwind CSS | v4 | CSS-first 配置，`@theme` 直接映射 demo 的设计变量，零 config 文件 |
| 动画 | motion（原 Framer Motion） | 12.x | `AnimatePresence` 出入场、`layout` 属性自动 FLIP，替代 demo 手写的 flip() |
| 拖拽 | @atlaskit/pragmatic-drag-and-drop（pdnd）+ hitbox + auto-scroll | 最新 | Atlassian 出品、Trello/Jira 生产在用、 actively maintained。跨容器拖拽（标签→书签树）是它的主场；dnd-kit 跨容器方案要自己粘合，放弃 |
| 状态 | zustand | v5 | 极轻、无 Provider 包裹，配 immer 写书签树 |
| 校验 | zod | v4 | JSON 批量导入的结构校验 |
| 测试 | Vitest + 手动 QA 清单 | — | chrome.* 依赖重，单测只覆盖纯逻辑（树操作、JSON 解析、URL 归一化） |

**不用**：UI 组件库（整页自定义风格，组件库只会碍事）、Redux（过重）、Webpack/CRACO（过时）。

---

## 二、扩展结构与目录

```
tabManager/
├─ manifest.config.ts          # crxjs 的 manifest 定义（TS 类型提示）
├─ src/
│  ├─ newtab/
│  │  ├─ index.html
│  │  ├─ main.tsx
│  │  └─ App.tsx               # 整页布局壳
│  ├─ components/
│  │  ├─ Topbar/               # 搜索、统计、时钟、帮助
│  │  ├─ QuickSites/           # 快捷站点九宫格 + 行内表单 + 拖拽落点（标签/书签拖上来即加入）
│  │  ├─ TabPanel/             # 标签卡片网格 + 工具条
│  │  ├─ BookmarkPanel/
│  │  │  ├─ BookmarkTree.tsx   # 左树
│  │  │  └─ BookmarkGrid.tsx   # 右卡片网格 + 面包屑 + 子文件夹 chips
│  │  ├─ ImportModal.tsx       # JSON 批量导入
│  │  ├─ FxLayer.tsx           # 粉碎粒子特效层
│  │  └─ Toast.tsx             # 带 action 按钮 + 倒计时条
│  ├─ dnd/
│  │  ├─ DndRoot.tsx           # 全局 DndContext（跨面板统一注册）
│  │  └─ dropTargets.ts        # 各 drop target 注册/高亮逻辑
│  ├─ store/                   # zustand slices
│  │  ├─ tabsSlice.ts
│  │  ├─ bookmarksSlice.ts     # 树结构 + 当前文件夹 + 展开/折叠
│  │  ├─ quickSlice.ts
│  │  └─ selectionSlice.ts     # 两套选中集合 + shift 范围选
│  ├─ services/
│  │  ├─ chromeTabs.ts         # tabs.* 封装
│  │  ├─ chromeBookmarks.ts    # bookmarks.* 封装
│  │  ├─ storage.ts            # chrome.storage.local 封装 + onChanged
│  │  └─ favicon.ts            # _favicon 端点 + 字母 tile 降级
│  ├─ lib/
│  │  ├─ bookmarkTree.ts       # 纯函数：树查找/移动/计数（可单测）
│  │  ├─ importParse.ts        # JSON → 书签列表（zod，可单测）
│  │  └─ url.ts                # normalizeUrl / hostOf（可单测）
│  └─ styles/
│     └─ theme.css             # Tailwind v4 @theme，迁移 demo 全部变量
└─ vite.config.ts
```

### manifest 关键内容（MV3）

```jsonc
{
  "manifest_version": 3,
  "name": "TabNest 标签巢",
  "version": "0.1.0",
  "permissions": ["tabs", "bookmarks", "storage", "favicon"],
  "chrome_url_overrides": { "newtab": "src/newtab/index.html" },
  "icons": { /* 16/32/48/128 */ }
}
```

- **不需要** background service worker：所有 chrome.* 调用直接在 newtab 页发起，事件监听也挂页面上（页面开着才需要响应事件，天然成立）。
- `favicon` 是较新权限（Chrome 104+），配合 `chrome-extension://<id>/_favicon/?pageUrl=…&size=32` 取真实站点图标，替代已废弃的 `chrome://favicon/`。

---

## 三、需求 → 实现映射总表

| # | 需求点 | 实现路径 | 关键 API / 库 |
|---|---|---|---|
| 1 | 点 + 号打开本插件 | `chrome_url_overrides.newtab` | MV3 |
| 2 | 快捷站点增删改、一键打开 | 数据存 `chrome.storage.local`，UI 表单行内增改；**无数量上限**；标签 / 书签卡片可直接拖到快捷访问区加入（按网址去重，源不动） | storage API |
| 3 | 标签卡片展示已打开标签 | 初次 `tabs.query({})` 渲染，之后靠事件流维持 | `tabs.onCreated/onUpdated/onRemoved/onReplaced/onMoved` |
| 4 | 点 × 关标签 + 粉碎动画 | 先播 FX（粒子 + 卡片 crush 动画，约 340ms）再调 `tabs.remove` | motion `AnimatePresence` + 手写 FxLayer（照搬 demo 算法） |
| 5 | 单击卡片新标签页打开 | `tabs.create({ url, active: true })` | tabs |
| 6 | 多选 / 全选批量关标签 | 选中集合（click / Ctrl+click / Shift 范围选 / 全选按钮），关闭前逐卡片收集 rect 播粉碎 | zustand selection slice |
| 7 | 卡片拖动排序 → 真实标签顺序同步 | pdnd sortable，drop 后按新顺序计算 `tabs.move`，**乐观更新 UI + 失败回滚** | pdnd + `tabs.move(tabId, { index })` |
| 8 | 标签拖到右侧 → 添加为书签 | 同一 DndContext 跨容器：drop target = 书签网格 / 树节点，成功后 `bookmarks.create` + 新卡片 flash 高亮 | pdnd + bookmarks |
| 9 | 书签树层级展示、收起展开 | `bookmarks.getTree()` 建树，展开状态存 storage.local | bookmarks |
| 9.1 | 文件夹管理（已确认要做）：树底部 **+ 新建子文件夹**（挂在当前选中文件夹下）、行内重命名（与书签同交互，blur 确认）、右键/悬停 × 删除（非空文件夹用带倒计时的确认 toast，不做原生 confirm 弹窗） | `bookmarks.create({ parentId, title })` / `bookmarks.update` / `bookmarks.remove`（递归删子树用 `removeTree`） | bookmarks |
| 9.2 | 书签/文件夹拖入文件夹作为子项 | 复用 4.2 的"→ 树节点行"drop target；书签与文件夹均可作为 draggable（文件夹不可拖进自身或其后代，drop 前校验并 toast 提示） | pdnd + bookmarks.move |
| 10 | 书签卡片（logo/名称/网址）——版式与左侧标签卡片完全一致（上排 logo + 名称两行截断，下排 勾选 + 域名 + 编辑图标，× 悬浮在右上角） | `_favicon` 取图，失败降级首字母彩色 tile | favicon |
| 11 | **只有**右下角编辑图标是编辑入口（名称/网址文字是纯展示，点了等于点卡片）→ 名称与网址**同时**变输入框，聚焦并全选名称；名称 blur 提交后光标自动链到网址并全选，网址 blur 提交收工；任一框为空时 blur **恢复原状不提交**；Esc 放弃；不弹窗不限字符 | 编辑态原位换 `<input>`（autoFocus 全选），`blur` / Enter 提交，Esc 取消；网址留空时显示占位提示 | 受控组件；编辑态禁用该卡片拖拽；编辑态失焦那一击不再触发「打开书签」 |
| 12 | 卡片 × 删除 + Ctrl+Z 撤回 | 删除前记录 `{ parentId, index, 节点快照 }` 入 undo 栈；`bookmarks.remove`；Ctrl+Z 或 toast 按钮触发 `bookmarks.create(restoreInfo)`（create 会原位还原） | bookmarks |
| 13 | 书签多选/全选批量删除 | 同 6 的选择交互，删除合并为一条 undo 记录（整批可一次撤回） | bookmarks.remove 循环 |
| 14 | 书签拖动排序 → 真实书签变化 | pdnd sortable，drop 后逐个 `bookmarks.move(id, { parentId, index })`，乐观更新 + 回滚 | pdnd + bookmarks.move |
| 15 | 面板顶部 + 新增卡片，光标聚焦名称 | push 空书签（乐观）→ `bookmarks.create` → 渲染后 `.focus()` 名称输入框，blur 时若名称和 url 都空则自动撤销该条 | bookmarks.create |
| 16 | JSON 文件批量新增 | 弹窗两种模式：粘贴 JSON / 拖文件进 dropzone；zod 校验 `{name,url}[]` 或 Chrome 导出的书签树（递归展平）；导入目标 = 当前文件夹 | zod + FileReader |
| 17 | 单击卡片任意位置（含名称/网址文字）都在新标签页打开；url 为空时转为进入编辑态 | `tabs.create({ url: b.url })` | tabs |
| 18 | 清新视觉、粉碎动画、FLIP | Tailwind v4 `@theme` 迁移 demo 全部 CSS 变量；grid 重排用 motion `layout`；粉碎用 demo 的粒子算法 | — |

---

## 四、核心模块设计

### 4.1 数据流与状态同步

**标签列表**：以 `chrome.tabs` 事件流为唯一事实来源。页面加载 `query({})` 初始化，之后 `onCreated/onRemoved/onUpdated/onMoved/onReplaced` 增量同步 → 不会出现"关了标签列表还显示"的脏数据。批量关闭时先批量 remove，等事件回灌后 UI 自动消失（FX 只负责视觉，不承担状态）。

**书签树**：同样事件驱动（`bookmarks.onCreated/onRemoved/onChanged/onMoved/onReordered`）。乐观更新策略：UI 先动（拖拽/编辑），API 调用失败则用快照回滚 + toast 报错。多开几个新标签页时各自靠事件流保持一致，无需额外通信。

**快捷站点 / UI 偏好**（展开状态、左右分栏宽度、帮助开关）：`chrome.storage.local`，写入后 `storage.onChanged` 广播，多窗口一致。

### 4.2 拖拽系统（pdnd，6 个场景一张表）

| 场景 | draggable | drop target | 完成动作 |
|---|---|---|---|
| 标签排序 | tab 卡片（支持携带整组选中） | 相邻 tab 卡片左右半区（hitbox `edge`） | 重排 UI → `tabs.move` 批量按序移动 |
| 标签 → 书签 | 同上 | 书签网格空区 / 具体卡片 / 树节点行 | `bookmarks.create` × N（挂在目标文件夹），新卡 flash |
| 书签排序 | bm 卡片（支持整组选中） | 相邻 bm 卡片半区 | 重排 UI → `bookmarks.move` |
| 书签 → 文件夹 | bm 卡片 | 树节点行（高亮 inset 边框） | `bookmarks.move({ parentId })` |
| 标签 / 书签 → 快捷访问 | tab / bm 卡片 | 快捷访问区整体（虚线框 + 「松手加入」提示条） | 纯本地：`addQuickSites`（按 URL 去重、补名，源不动） |
| JSON 文件拖入 | 系统文件 | 书签面板整体 | 打开导入弹窗并预填 |

多选拖拽：drag start 时若起点卡片在选中集合内，则携带 `selectedIds`（保持当前顺序），drop 后整组按相对顺序落到目标位——这正是需求"多选多个拖拽"的实现。拖拽与点击的冲突用**位移阈值**（>4px 才算 drag）区分，demo 已验证此手感。

`@atlaskit/pragmatic-drag-and-drop-auto-scroll` 负责拖到网格边缘时自动滚动（书签多时必需）。

### 4.3 动画系统

- **入场**：首屏卡片 stagger（motion `variants`），新卡片单卡入场。
- **重排 FLIP**：网格容器内所有卡片加 `layout` prop，顺序变化自动补间，替代 demo 手写 flip()。
- **关闭粉碎**：保留 demo 方案——卡片 `cardCrush`（缩放+旋转+模糊）+ FxLayer 生成 20~30 个随机碎片飞散（每卡片颜色取自 tile 渐变色）。批量关闭时全部卡片同时播，动画 340ms 后统一提交 chrome API。
- **删除撤回**：还原后卡片 `flash` 高亮一圈（demo 已有）。
- **面板/弹窗**：`AnimatePresence` 统一管理退出动画，杜绝"删除后直接消失"的生硬感。

### 4.4 行内编辑（blur 确认，入口=右下角编辑图标）

点卡片右下角的**编辑图标** → 名称与网址同时替换为无边框 input（样式与文字一致，聚焦环用 jade 色），
名称框 autofocus + 全选。**名称/网址文字本身不是入口**——它们是纯展示元素，点击冒泡给卡片 = 打开书签。

- 名称 blur / `Enter` → 提交 `bookmarks.update(id, { title })`，同时把光标自动交给网址框并全选；
  网址 blur / `Enter` → 提交并退出编辑态。空值（含纯空格）blur → **不提交、恢复原状**（草稿两栏都空则自动撤销该条）。
- `Esc` → 放弃本次编辑（两栏一起还原）。
- 编辑中冻结该卡片的 draggable，防止拖一半变输入；编辑结束的那一次点击用 `swallowed` ref 吞掉，
  避免「失焦收工」和「打开书签」两件事在同一击里一起发生。
- 名称/网址不限制字符，不做任何校验（需求明确），仅网址在打开时才做 URL 归一化（无协议自动补 `https://`）。
- 名称/网址都不再有 hover 下划线提示（那是给「点文字改内容」用的，交互已移除）。

### 4.5 撤销（Ctrl+Z）

内存 undo 栈，每条记录：`{ type:'delete', items: [{ parentId, index, node }] }`（批量删除是同一条记录，一次撤回整批）。

- 触发：全局 `keydown`（Ctrl/Cmd+Z，输入框聚焦时除外）+ 删除 toast 上的"撤回"按钮（带 6s 倒计时条，demo 样式）。
- 撤回 = 对每条 item 按 `parentId + index` 调 `bookmarks.create(restoreInfo)`（bookmarks.create 支持传 index，原位还原），随后 flash 高亮。
- **多级连续撤回（已确认）**：栈实现，Ctrl+Z 可逐级回退，每步都有对应 redo 不做（超出需求不扩展）。
- **边界**：undo 栈只收书签与文件夹的删除操作；**删除文件夹（removeTree）不入栈**（整棵子树还原成本高且有丢失风险），删除前用带倒计时的确认 toast 把关；标签关闭不做 undo（重开一个新标签即可）。

### 4.6 JSON 批量导入

- 入口：面板顶部按钮弹窗（粘贴 / 选文件）+ 直接拖 `.json` 文件进书签面板。
- zod schema 双格式兼容：① `{name,url}[]` 或 `{list:[…]}`（demo 格式）② Chrome 书签管理器导出的树（递归收集所有 url 节点）。
- 校验后显示"N 条可导入，M 条无效跳过"，确认后批量 `bookmarks.create` 到当前文件夹，新卡片 stagger 入场。
- 失败（格式完全不对）：toast 报错 + 显示示例 JSON（demo 的 SAMPLE）。

### 4.7 图标策略

1. 首选 `_favicon/?pageUrl=<url>&size=32`（favicon 权限）。
2. 加载失败 / chrome 内部页 → 首字母彩色渐变 tile（demo 的 `colorFor` 哈希配色，直接保留，这也是"缤纷"感的来源之一）。

---

## 五、权限与安全

| 权限 | 用途 | 备注 |
|---|---|---|
| `tabs` | 读取/移动/关闭标签 | 需求核心 |
| `bookmarks` | 书签 CRUD | 需求核心 |
| `storage` | 快捷站点与 UI 偏好 | — |
| `favicon` | 站点图标 | Chrome 104+，无需 host_permissions |

无 host_permissions、无 remote code、无外部请求（图标走浏览器内部端点），商店审核风险低。

---

## 六、关键风险与对策

| 风险 | 对策 |
|---|---|
| **拖拽排序与真实顺序映射**：网格是二维布局，`tabs.move` 是一维 index。多选拖拽一次 move 多个，中间态 index 会互相顶位 | 统一算法：目标插入点 = 命中卡片在"剩余项"中的位置；先移出被拖项、再按序逐个 move（demo 的 rest/moving 分离思路，写成纯函数 + 单测） |
| **事件回灌与乐观更新打架**：move/create 的事件回来时 UI 已更新，可能重复渲染 | 所有 chrome 回调写 store 前先做幂等检查（id 已存在/顺序已一致则跳过） |
| **chrome://、edge:// 等内部页**：tabs.create 打不开、favicon 取不到 | 卡片可展示但点击静默失败 + toast 提示；图标走字母 tile |
| **键盘焦点**：新标签页打开后焦点可能不在页面 | `main.tsx` 自动 focus 搜索框；`/` 聚焦搜索、`Esc` 清空、`Ctrl+Z` 撤销，均避开输入框 |
| **超大树/大量书签**（几百上千条） | 当前文件夹才全量渲染，树只渲染可见层级；卡片数 > 300 再上虚拟列表（预留，不预做） |
| **crxjs 2.x 仍是 beta** | 备选方案 `vite-plugin-web-extension`；manifest 结构简单，迁移成本低 |

---

## 七、里程碑

1. **M1 骨架**（可加载的空壳）：crxjs + React + Tailwind 主题迁移，newtab 生效，布局三分栏（左右面板 + 树）。
2. **M2 只读展示**：标签列表 + 书签树/卡片 + favicon + 搜索 + 时钟/统计，事件流同步。
3. **M3 基础操作**：开关标签（含粉碎动画）、书签增删改（行内编辑 + 多级 Ctrl+Z）、**文件夹新建/重命名/删除（含确认 toast）**、快捷站点 CRUD、批量选择。
4. **M4 拖拽**：pdnd 全场景（排序、跨面板、多选拖拽、书签/文件夹拖入文件夹、文件拖入）+ 自动滚动。
5. **M5 收尾**：JSON 导入、帮助面板、空态、图标、QA 清单过一遍、打包 zip。

---

## 八、已确认决议（2026-09-17，龙哥拍板）

1. **书签文件夹操作：做**。支持新建/重命名/删除文件夹 + 书签及文件夹拖入文件夹（见 4.2 场景表与映射表 9.1/9.2）。
2. **Ctrl+Z 深度：多级栈**。书签/文件夹删除可逐级撤回；文件夹整树删除不入栈，用确认 toast 把关（见 4.5）。
3. **暗色模式：先不做**。本版纯浅色奶油风；后续如加，走 `prefers-color-scheme` + CSS 变量切换，架构上 Tailwind v4 `@theme` 已天然预留。
4. **快捷站点：数量上限已移除**（2026-09-20 龙哥要求）。原「软上限 16」作废，新建不再拦截；标签 / 书签卡片可直接拖进快捷访问区加入。囤多了由 `.quick-grid` 自己滚（`max-height: 34vh`），不挤占标签列表。
