# 开发一个 Chrome 浏览器扩展：新标签页标签管理 + 书签管理

## 一、总体目标

开发一个谷歌浏览器插件：点击标签栏的 **+** 号（新建标签页）时，打开本插件的页面。

## 二、整体布局

采用左右布局：

- **左侧**：已打开标签页的管理
- **右侧**：浏览器真实书签的管理

## 三、左侧：已打开标签页管理

左侧再分为上下两部分。

### 1. 上部（占比小）

- 可自定义一些常用网站，支持快捷打开
- 支持增、删、改

### 2. 下部（占比大）

- 管理已经打开的标签网页，以好看的卡片式方式展示
- 点击卡片右上角的 **×** 即可关闭该标签，关闭时配合好看的消失粉碎动画
- 单击可以新标签页打开相应网站
- 支持多选 / 全选后批量关闭标签
- 支持按住某个卡片拖动排序，排序之后网页的真实顺序随之改变
- 支持单个或多选多个已打开的标签页拖拽到右边，直接添加进书签

## 四、右侧：书签管理

该页面布局同样为左右结构：

### 1. 左侧：树状菜单（占比小）

- 层级化显示所有书签文件夹
- 支持收起 / 展开

### 2. 右侧：书签展示页面（占比大）

- 用卡片式展示每个书签，展示网站 icon、名称、域名
- 修改网站名称、域名：直接点击相应位置即显示输入框直接输入修改，**blur 即确定**，不要弹窗（体验不好），不限制字符
- 卡片直接显示 **×** 号，点击即删除，并加上 **Ctrl + Z** 撤回功能，可撤回最近一次删除
- 支持多选 / 全选后批量删除
- 支持按住鼠标拖动排序，然后真实书签随之变化
- 支持新增：在这个版面上方放一个 **+** 号，点击后直接在最底部新增一个卡片，光标直接聚焦到名称输入处
- 支持批量新增：传入一个 **json** 文件即可
- 单击可以新标签页打开相应网站

## 五、视觉风格

整体 UI 清新简洁，美观优雅，配色缤纷，有设计感，没有 AI 味。
